<h1><%= @team.name %></h1>
<h3><%= @team.city %></h3>

<% if @team.coordinator %>
  <h4>Here are the coordinators for this team:</h4>
  <p> <%= link_to @team.coordinator.name, team_coordinator_path(@team, @team.coordinator) %> </p>


<% else %>
  <p>No coordinators are listed for this team.</p>

  <p><%= link_to "Add a coordinator", new_team_coordinator_path(@team) %></p>
  
<% end %>



<p><%= link_to "Edit", edit_team_path(@team) %></p>
<p><%= link_to "Delete", team_path(@team),
                         :data => {:confirm => "You sure?",
                                   :method => "delete"} %></p>

<p><%= link_to "Return to teams", teams_path %></p>